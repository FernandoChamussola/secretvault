# Database Configuration
DB_ROOT_PASSWORD=root
DB_PASSWORD=secret

# JWT Secret (for token signing)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# CRITICAL: Master Encryption Key
# Generate with: openssl rand -hex 32
# This key encrypts ALL secrets in the database
# KEEP THIS SAFE - losing it means losing access to all encrypted data
# Anyone with this key can decrypt all secrets
MASTER_KEY=

# Alternative: Use Docker secrets instead of environment variable
# Create file: ./secrets/master_key.txt with your key
# Then comment out MASTER_KEY above
