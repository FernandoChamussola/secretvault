# Database - MySQL
DB_ROOT_PASSWORD=your_secure_root_password_here_change_this
DB_PASSWORD=your_secure_db_password_here_change_this

# JWT (at least 32 characters recommended)
JWT_SECRET=your_super_secret_jwt_key_here_min_32_chars_recommended_change_this

# Encryption (must be exactly 32 characters)
ENCRYPTION_KEY=12345678901234567890123456789012

# Frontend URL (for CORS)
FRONTEND_URL=https://cofrekeys.duckdns.org

# Note: Generate secure values with:
# openssl rand -base64 32  (for passwords and JWT)
# openssl rand -hex 16     (for encryption key - exactly 32 chars)
